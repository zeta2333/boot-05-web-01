spring:
  #  mvc:
  #      static-path-pattern: /res/**
  web:
    resources:
      add-mappings: true
      cache:
        period: 1234

  mvc:
    hiddenmethod:
      filter:
        enabled: true
    contentnegotiation:
      favor-parameter: true # 开启基于请求参数(?param)的内容协商功能
server:
  servlet:
    context-path: /hello
#debug: true